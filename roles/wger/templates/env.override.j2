# external-file
# owner: wger-project
# repo: docker
# path: config/prod.env
# commit-hash: 2bf2b1e3e86c355a89e5c7f8e99a2846529d6f9b

SECRET_KEY={{ wger_secret_key }}
SIGNING_KEY={{ wger_signing_key }}
TIME_ZONE={{ timezone }}
TZ={{ timezone }}
CSRF_TRUSTED_ORIGINS=https://{{ services.wger.domain }}

#
# Static files
# If you are running the application behind a reverse proxy or changed the port, the
# links for some images *might* break (specially in the mobile app). Also note that
# the API response is cached and contains the host, if you change this setting, just run
# docker compose exec web python3 manage.py warmup-exercise-api-cache --force
# MEDIA_URL=https://your-domain.example.com/media/
# STATIC_URL=https://your-domain.example.com/static/

#
# Application
ALLOW_REGISTRATION={{ wger_allow_registration }}
ALLOW_GUEST_USERS={{ wger_allow_guest_users }}
# Users won't be able to contribute to exercises if their account age is
# lower than this amount in days.
MIN_ACCOUNT_AGE_TO_TRUST=0
CELERY_FLOWER_PASSWORD={{ wger_celery_flower_password }}
DJANGO_DB_PASSWORD={{ wger_db_password }}
SITE_URL=https://{{ services.wger.domain }}

#
# Email
# https://docs.djangoproject.com/en/4.1/topics/email/#smtp-backend
ENABLE_EMAIL=True
EMAIL_HOST={{ no_reply_mail }}
EMAIL_PORT={{ smtp_port }}
EMAIL_HOST_USER={{ no_reply_mail }}
EMAIL_HOST_PASSWORD={{ smtp_password }}
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
FROM_EMAIL='wger Workout Manager <{{ no_reply_mail }}>'
