services:
  zerobyte:
    image: ghcr.io/nicotsx/zerobyte:v0.32.0@sha256:0dbefe5d2d9a0f22b5206713f01df6fa84c47ff0a7f70ebf447d20ca39fa7cc1
    container_name: "{{ services.zerobyte.container_name }}"
    restart: always
    networks:
      - caddy
    environment:
      TZ: "{{ timezone }}"
      BASE_URL: "https://{{ services.zerobyte.domain }}"
      APP_SECRET: "{{ zerobyte_app_secret }}"
      TRUSTED_ORIGINS: "https://{{ services.pocket_id.domain }}"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - "{{ zerobyte_data_dir }}/data:/var/lib/zerobyte"
      - "/root/.config/rclone:/root/.config/rclone"
      - "{{ backup_base_dir }}:{{ backup_base_dir }}"

networks:
  caddy:
    external: true
