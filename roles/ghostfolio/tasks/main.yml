---
- name: Deploy Ghostfolio
  ansible.builtin.include_role:
    name: service_deploy
  vars:
    service_name: "Ghostfolio"
    service_dir_name: "{{ ghostfolio_dir_name }}"
    compose_url: "https://raw.githubusercontent.com/ghostfolio/ghostfolio/refs/heads/main/docker/docker-compose.yml"
