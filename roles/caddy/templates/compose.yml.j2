services:
  caddy:
    image: caddybuilds/caddy-cloudflare:2.11.3@sha256:79a418c615798b95d79b5856c15807d2815e048634bfadff138e9b5382b9f0b5
    container_name: caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    cap_add:
      - NET_ADMIN
    networks:
      - caddy
    volumes:
      - "{{ caddy_service_dir }}/Caddyfile:/etc/caddy/Caddyfile:ro"
      - "{{ caddy_data_dir }}/data:/data"
      - "{{ caddy_data_dir }}/config:/config"

      # wger file server volumes
      - "{{ wger_data_dir }}/static:/srv/wger/static:ro"
      - "{{ wger_data_dir }}/media:/srv/wger/media:ro"
    environment:
      CLOUDFLARE_API_TOKEN: "{{ cloudflare_api_token }}"

networks:
  caddy:
    name: caddy
