services:
  caddy:
    image: caddybuilds/caddy-cloudflare:2.11.1@sha256:34eb54922fce79cfa285b9177116f7a19295080878a2c7ea62f880c6d712991b
    container_name: caddy
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    cap_add:
      - NET_ADMIN
    networks:
      - caddy
    volumes:
      - "{{ caddy_service_dir }}/Caddyfile:/etc/caddy/Caddyfile:ro"
      - "{{ caddy_data_dir }}/data:/data"
      - "{{ caddy_data_dir }}/config:/config"

      # wger file server volumes
      - "{{ wger_data_dir }}/static:/srv/wger/static:ro"
      - "{{ wger_data_dir }}/media:/srv/wger/media:ro"
    environment:
      CLOUDFLARE_API_TOKEN: "{{ cloudflare_api_token }}"

networks:
  caddy:
    name: caddy
