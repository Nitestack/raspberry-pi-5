---
- name: Deploy NextCloud
  ansible.builtin.include_role:
    name: service_deploy
  vars:
    service_name: "NextCloud"
    service_dir_name: "{{ nextcloud_dir_name }}"
    compose_url: "https://raw.githubusercontent.com/nextcloud/all-in-one/refs/heads/main/compose.yaml"
